/**
 * Next.js runtime/build configuration.
 *
 * Keep this file intentionally minimal and explicit so future contributors
 * can quickly identify which framework-level behaviors were customized.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
  output: 'export'
};

export default nextConfig;
